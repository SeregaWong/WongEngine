import {El} from './El';
import {WongEngine} from './WongEngine';

const {
    create,
    getElClassByName,
    registerClass,
    schemaParsers,
    setSchemaParser,
} = WongEngine;

export default {
    create,
    getElClassByName,
    registerClass,
    schemaParsers,
    setSchemaParser,
    El,
}
