import {El} from './El';
import {WongEngine} from './WongEngine';

const {
    create,
    getElClassByName,
    registerClass,
} = WongEngine;

export default {
    create,
    getElClassByName,
    registerClass,
    El,
}
